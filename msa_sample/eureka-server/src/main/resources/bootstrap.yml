spring:
  application:
    name: eureka-server
  profiles:
    active: local
  cloud:
    config:
      url: http://localhost:8888
    loadbalancer:
      ribbon:
        enabled: false
                
server:
  port: 8761