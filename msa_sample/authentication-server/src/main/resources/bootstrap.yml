server:
  port: 8900
spring:
  application:
    name: authentication-server
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8888
    loadbalancer:
      ribbon:
        enabled: false